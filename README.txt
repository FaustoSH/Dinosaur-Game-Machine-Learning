Para poder inicializar instalar en un entorno de ejecución de python o en tu ordenador:
pip install selenium torch torchvision numpy opencv-python

Además, en la carpeta raiz, junto al main, copiar chromedriver.exe